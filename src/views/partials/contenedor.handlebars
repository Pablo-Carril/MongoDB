<div class="container-fluid ">
  <div class="row" style="flex-wrap: nowrap;">

    {{!-- {{#if mostrarLoading}}
    {{> !--loading}}
    {{/if}} --}}

    <div class="col-2" style="min-width: 240px;"> <!-- este maneja el ancho del formulario. y también ancho mínimo-->
      {{> formulario}} <!--todos los que se llamen de este modo deben encontrarse en partials-->
      <!-- y para actualizarce debe actualizarce toda la página      text-danger  alert alert-primary   -->
    </div>

    <div class="col-9 border" style="min-width: 200px;">

      <h5 class="mt-2 p-2" style="border: 1px solid #86b7fe; border-radius: 8px; box-shadow: 0px 0px 2px 2px #86b7fe" >{{resultadosDe}} 
        <span class="ms-2 fw-bold " style="font-size: 1.1rem; color: {{colorOpcion this.equipo}};"> {{busqueda}} </span> 
      </h5>
    

      {{#if mostrarHistorial}}
      {{> historial}}
      {{/if}}

      {{#if mostrarUltimos}}
      {{> ultimos}}
      {{/if}}

      

    </div>

    <!--  <div id="nota"></div> -->

  </div>
</div>

