<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary mt-2 mb-3" style="flex-wrap: nowrap;">
    <div class="container-fluid">

      <div class="navbar-header col-3">
        <img src="/img/SaesSUBE2.png" alt="Logo" class="img-fluid" id="logo">
        <a class="navbar-brand" href="/ultimos" id="titulo">Control de EQUIPOS</a>
      </div>

      <div class="col-6">
        <ul class="navbar-nav flex-row">
          <li class="nav-item"> <!--validadores sólo debe switchear el selector, no llamar a un router-->
            <a class="nav-link active reset" href="/ultimos">Ultimos</a> <!--colocar el active en el que se seleccione-->
          </li>
          <li class="nav-item"> <!--Teclado sólo debe switchear el selector, no llamar a un router-->
            <a class="nav-link active ms-3" href="/sonda">Sonda</a>
          </li>
          <li class="nav-item"> <!--Teclado sólo debe switchear el selector, no llamar a un router-->
            <a class="nav-link active ms-3" href="/laplata">La Plata</a>
          </li>
          <li class="nav-item"> <!--Teclado sólo debe switchear el selector, no llamar a un router-->
            <a class="nav-link active ms-3" href="/notas">Ayuda</a>
          </li>
          <li class="nav-item">
            <a class="nav-link ms-3 active" href="/about" id="Acerca_de">Acerca de</a>
            <!-- debería seguir viéndose el nav -->
          </li>
        </ul>
      </div>
      <form action="/busqueda" method="post" class="d-flex col-3" role="search">
        <input name="datosBuscar" class="form-control me-2" type="search" placeholder="Problema/Caso/Coche/etc.." aria-label="Buscar" id="buscarInput">
        <button type="submit" class="btn btn-outline-light my-2 my-sm-0">Buscar</button>
      </form>
    </div>
  </nav>
</header>

<script>
 // const reset = document.querySelectorAll('.reset')
 // reset.forEach(link => {
 // link.addEventListener('click', (event) => {
 //   document.getElementById("formEntrada").reset()
 //  })
 // })
/* document.addEventListener('DOMContentLoaded', () => {  //FUNCIONA! pero no si viene de otra página...tal vez en un nivel superior como main
  const lastRouteLink = document.querySelector('.nav-link');
  const spinnerContainer = document.querySelector('.spinner-container');
  lastRouteLink.addEventListener('click', (event)=> {
  event.preventDefault();
  const route = event.target.href;
  spinnerContainer.classList.remove('escondido') // Muestra el Spinner. cuando se actualize la página se oculta solo.
  window.location.href = route
  })
}) */

</script>